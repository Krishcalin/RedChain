# RedChain
A Python engagement orchestrator that runs red-team operations as a gated state machine — one phase, one specialist agent, one validated artifact at a time. Built on the Anthropic SDK with prompt caching, designed for multi-day engagements with checkpoint/resume.
